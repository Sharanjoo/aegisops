apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage 2 of the deployment order: MySQL and Kafka must be up (and their
# Services resolvable) before the Kafka topic-init Job or any application
# workload is applied.
resources:
  - ../../../base/mysql
  - ../../../base/kafka
