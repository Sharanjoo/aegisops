apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage 3 of the deployment order: a one-off Job that idempotently
# provisions aegisops.incident.events.v1 once Kafka is reachable.
resources:
  - ../../../base/kafka-topic-init
