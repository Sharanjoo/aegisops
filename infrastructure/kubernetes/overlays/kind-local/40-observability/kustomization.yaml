apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Stage 5 of the deployment order: local-development-only Prometheus,
# scraping incident-service, realtime-gateway, and cluster-agent (all
# already up from stage 30-application) via static in-cluster targets.
resources:
  - ../../../base/prometheus
